// We want this component to render our prizes and to potentially sort our prize winners
// We should be able to sort our winners by year and last name (if multiple winners for a single award,
// order the winners for that one award, then use the first winner in the list for your sort value.

function PrizeContainer() {
  return (
    <div></div>
  )
}